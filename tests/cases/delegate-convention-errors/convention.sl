// A closure is a pointer and a receiver, so there is no foreign function on the
// other end of one and a convention would describe nothing.
module Convention;

public closure __stdcall int Handler(int value);

int Main() { return 0; }
