# 15: a leak, and not a static -- this case declares none, so what is here
# was allocated and never let go of. Seven `FileStream` and what they held. Recorded so the
# number stops moving rather than because it is right. See TODO.md.
15
