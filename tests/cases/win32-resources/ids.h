// Included by resources.rc. llvm-rc resolves an #include against the script's
// own directory, so this is found wherever the build was started from.
#define IDS_FIRST    201
#define IDS_SECOND   202
#define IDR_PAYLOAD  301
