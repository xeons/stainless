# A member read inside a lambda is a copy, and these two members are written
# elsewhere -- so the lambda reads what they said when it was made. `Fixed` and
# `Correct` are here to show what does not warn: a member only a constructor
# writes, and a member reached through a method call.
SL0610
