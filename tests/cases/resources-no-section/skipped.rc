// A resource script in a program built for a machine that has no resource
// section. It is left out, and the build says so rather than quietly producing
// a binary without it.
STRINGTABLE
BEGIN
    1 "this never reaches an ELF binary"
END
