# 2: the two objects `Reflection.CreateInstance` made. It answers with a
# reference the caller owns and the module keeps `sl_release` to itself, so
# there is no way to give it back -- the same gap `CreateArrayInto` was added
# to close for arrays. See TODO.md.
2
