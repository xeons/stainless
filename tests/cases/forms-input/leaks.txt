# 5: what the Win32 backend keeps for the life of the program -- the menu-id
# table, the custom-colour table, and the queue another thread posts work on.
# Each is a `static readonly`, and so immortal rather than leaked.
5
