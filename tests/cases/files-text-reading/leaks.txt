# 6: a leak, and not a static -- this case declares none, so what is here
# was allocated and never let go of. Streams and their buffers. Recorded so the
# number stops moving rather than because it is right. See TODO.md.
6
