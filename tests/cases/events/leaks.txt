# 3: a reference cycle, and the known one. An event holds its subscriber
# weakly only where the handler is bound to `this`; wired by anyone else it
# takes the strong path, and an object that also holds the event is then never
# freed. See TODO.md.
3
