// SPDX-License-Identifier: 0BSD
//
// The other half. Nothing names this file anywhere: it is found because the
// project says `sources` is `src`, which is the whole point.
module Fixture;

String FormatGreeting() => "the fixture built";
